import { Landing } from '@layouts/index';
import { NotFound } from '@view/landing';

const NotFoundPage = () => {
  return (
    <Landing>
      <NotFound />
    </Landing>
  );
};

export default NotFoundPage;
