import { Landing } from '@layouts/index';
import { PrivacyPolicy } from '@view/landing';

const MainPage = () => {
  return (
    <Landing>
      <PrivacyPolicy />
    </Landing>
  );
};

export default MainPage;
